# Satellite-Random-Spherical-Hypergraphs
This paper introduces Sat-RSH, a random spherical hypergraph model for satellite-like group coverage. It studies how target hyperedge sizes, random capture, and duplicate removal affect degree, connectivity, clustering, and path length. Results show that spherical localization produces higher clustering than a size-matched non-geometric model.
